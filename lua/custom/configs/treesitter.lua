local options = {
  ensure_installed = { "typescript", "javascript", "tsx", "prisma", "python", "lua", "html", "css", "json", "json5", "vim", "vimdoc" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
